package com.felipeb.discordclone.chat.channel;

public enum ChannelPermission {
    /** Any member of the server can read and write. */
    READ_WRITE,
    /** Members can read but only ADMIN+ can post. */
    READ_ONLY
}
